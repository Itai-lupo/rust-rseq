## todo
let the user write a per cpu macro that will compile into the rseq lib

that macro should have abort handler code and rseq code

use proc-macro for that


the goal is the have a PerCpu struct that "just work TM"
